namespace GodotStsXcomalike.src.ironlordbyron.CSharp.BattleEntities.Enemies.Slimes
{
    /// <summary>
    /// Amoebas gain resistance to cards from classes that it has faced in prior encounters.
    /// Mechanically: each time it's damaged, it takes note of who damaged it.  At the beginning of the NEXT combat, starts
    /// with a buff that absorbs the first X damage from those classes.
    /// Growth mechanic: upgrades visually and gains buff if not killed within 3 turns.
    /// 
    /// </summary>
    public class Slimes
    {

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}